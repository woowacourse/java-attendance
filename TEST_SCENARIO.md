# 테스트 시나리오 (사용자 관점)


## 출석 확인 기능 시나리오
### Given 
- 사용자가 출석부에 등록된 상황에서

### When
1. 출석 시간에 확인을 하는 경우
2. 지각 시간에 확인을 하는 경우
3. 결석 시간에 확인을 하는 경우
4. 캠퍼스 출석 시간이 아닌 시간에 확인을 하는 경우

### Then
1. 출석 날짜, 요일, 시간을 출력하고, "출석"이라는 상태를 보여준다.
2. 출석 날짜, 요일, 시간을 출력하고, "지각"이라는 상태를 보여준다.
3. 출석 날짜, 요일, 시간을 출력하고, "결석"이라는 상태를 보여준다.
4. "출석 시간이 아니다"라는 예외 메시지를 출력하고, 출석 시간을 다시 입력 받는다.

---
### Given
- 사용자가 출석부에 등록되지 않은 상황에서

### When
1. 출석 확인 기능을 사용하려는 경우

### Then
1. "존재하지 않는 크루입니다."라는 예외 메시지를 출력하고, 크루 이름을 다시 입력 받는다.



## 출석 수정 기능 시나리오
### Given
- 사용자가 출석부에 등록된 상태에서

### when
1. 출석이 가능한 날짜, 출석이 가능한 시간으로 출석을 변경하는 경우
2. 출석이 불가능한 날짜의 출석을 변경하려는 경우
3. 출석이 불가능한 시간으로 출석을 변경하려는 경우
4. 존재하지 않는 날짜의 출석을 변경하려는 경우

### Then
1. 이전 출석의 날짜, 요일, 시각, 상태를 보여주고, 변경된 시각, 상태, 그리고 "수정 완료"라는 메시지를 보여준다.
2. "캠퍼스 등교일이 아닙니다."라는 메시지를 보여주고 날짜를 다시 입력받는다.
3. "캠퍼스 운영 시간이 아닙니다."라는 메시지를 보여주고 시간을 다시 입력받는다.
4. "잘못된 날짜입니다."라는 메시지를 보여주고 날짜를 다시 입력받는다.
---
### Given
- 사용자가 출석부에 등록되지 않은 상황에서

### When
1. 출석 수정 기능을 사용하려는 경우

### Then
1. "존재하지 않는 크루입니다."라는 예외 메시지를 출력하고, 크루 이름을 다시 입력 받는다.


## 크루별 출석 기록 확인 기능 시나리오
### Given
- 사용자가 출석부에 등록된 상황에서

### When
1. 크루별 출석 기록을 확인하는 경우

### Then
1. 다음의 내용을 사용자에게 보여준다.
   1. "이번 달 ~~의 출석 기록입니다."를 보여준다.
   2. 오늘을 제외한 어제까지의 출석 기록(날짜, 요일, 시각, 상태)를 보여준다.
      - 만약 출석 기록이 존재하지 않는다면, 결석으로 처리하고 "--:--"을 시간으로 처리한다.
   3. 이번달 출석 기록 상태(출석, 지각, 결석)의 통계를 사용자에게 보여준다.
   4. 만약 제적 위험 대상자라면, 제적 위험 대상자 상태(경고, 면담, 제적)를 사용자에게 보여준다.
---
### Given
- 사용자가 출석부에 등록되지 않은 상황에서

### When
1. 크루별 출석 기록을 확인하는 경우

### Then
1. "존재하지 않는 크루입니다."라는 예외 메시지를 출력하고, 크루 이름을 다시 입력 받는다.

## 제적 위험자 확인 기능 시나리오

### Given
- 어느 경우에서든, 

### When
1. 제적 위험자들을 확인하는 경우

### Then
1. 다음의 내용을 사용자에게 보여준다.
   1. "제적 위험자 조회 결과"를 보여준다.
   2. "닉네임: 결석 x회, 지각 x회 (제적 위험 대상자 상태)"를 사용자에게 보여준다.
   3. 2번의 데이터를 정렬하여 보여준다.
      - 지각 3회를 결석 1회로 간주하여 결석 횟수를 기준으로 내림차순한다.
      - 만약 위의 계산에 따른, 결석 횟수가 동률이라면 이름을 기준으로 사전 정렬한다.
